<!DOCTYPE html>
<html lang="{{ props.language }}" dir="ltr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% block stylesheets %}
    <link rel="stylesheet" href="{{ nestedness }}css/base.css">
    <link rel="stylesheet" href="{{ nestedness }}css/custom.css">
    {% for stylesheet in styles %}
    <link rel="stylesheet" href="{{ nestedness }}css/{{ stylesheet }}.css">
    {% endfor %}
    {% endblock %}
    <title>{{ this_page }} | {{ props.title}}</title>
</head>
<body>
    <header>
        <nav>
            <!-- https://thenounproject.com/term/scale/1320645/ -->
            <a href="https://nl.wikipedia.org/wiki/Pareidolie">
                <img src="{{ nestedness }}images\logo.svg" alt="logo">
            </a>
            <div class="header__title">{{ props.title }}</div>
            <input type="checkbox" id="nav__toggle" class="nav__toggle">
            <label for="nav__toggle" class="nav__toggle__button">☰</label>
            <div class="nav__menu">
                <ul>
                    {% for section, href in hrefs_sections.items() %}
                    <li><a href="{{ nestedness }}{{ href }}" {% if section==this_section %}class="nav__active" {% endif %}>{{ section }}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </nav>
        <div>
            <div>{{ breadcrumbs }}</div>
            {% block navigation %}{% endblock %}
        </div>
    </header>
    {% block aside %}{% endblock %}
    <main>
        <article>
            <h1 class="page__title">{{ this_page }}</h1>
            {% block content %}{% endblock %}
            <div class="timestamps">
                Aangemaakt: {{ ctime }} | laatst gewijzigd: {{ mtime }}
            </div>
        </article>
        <footer>
            <div class="footer__container">
                <div class="footer__container__contact">
                    <h3>Contact</h3>
                    <div><b>Centrale Studentenadministratie</b></div>
                    <div>Mail: <a href="mailto:matching.csa@uu.nl">matching.csa@uu.nl</a><br/>
                    </div>
                </div>
                <div class="footer__container__info">
                    <h3>Informatie</h3>
                    <div><a href="https://students.uu.nl/files/uu-reglement-inschrijving-en-collegegeld-2021-2022pdf">Reglement inschrijving en collegegeld</a></div>
                    <div><a href="https://www.uu.nl/bachelors/algemene-informatie/informatie-voor-nederlandse-studenten/inschrijven/matching/veelgestelde-vragen-over-matching">Matching FAQ</a></div>
                </div>
                <div class="footer__container__version">
                    <h3>Versie</h3>
                    <div><a href="{{ nestedness }}home/sitemap.html">Sitemap</a></div>
                    <div>Versienummer: <a href="{{ nestedness }}home/changelog.html">{{ props.version }}</a></div>
                    <div>Gemaakt met deze <a href="https://github.com/lcvriend/responsive_static_site_builder">tool</a></div>
                </div>
            </div>
            <div class="footer__logo">
                <img alt="Universiteit Utrecht" src="https://www.uu.nl/themes/custom/corp/src/images/logo_footer-nl.svg"
                    width=300>
            </div>
        </footer>
    </main>

{% block scripts %}{% endblock %}
</body>
</html>
{{ watermark }}
